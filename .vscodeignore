# Package only the bundled runtime and the metadata required by VS Code and
# the Marketplace. New repository files stay out of the VSIX by default.
**
!dist/**
!package.json
!package.nls.json
!package.nls.zh-cn.json
!icon.png
!README.md
!CHANGELOG.md
!LICENSE
