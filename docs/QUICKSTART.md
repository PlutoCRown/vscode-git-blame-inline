# 快速开始指南

## 5 分钟上手

### 1. 启动调试（30 秒）

在 VSCode 中打开本项目，按 `F5` 键。

这会：
- ✅ 自动编译代码
- ✅ 打开新的 VSCode 扩展开发窗口

### 2. 测试功能（2 分钟）

在新打开的 VSCode 窗口中：

#### 测试 1：查看行内 Blame 信息

1. 打开 `test-file.ts`（或任意已提交的文件）
2. 查看每行代码末尾的灰色文本
3. 应该显示：`作者, 时间 • 提交信息`

#### 测试 2：查看悬停详情

1. 将鼠标悬停在任意代码行上
2. 应该弹出包含以下信息的提示框：
   - Commit Hash
   - 作者
   - 邮箱
   - 时间
   - 完整提交信息

#### 测试 3：切换显示

1. 按 `Cmd+Shift+P`（Mac）或 `Ctrl+Shift+P`（Windows/Linux）
2. 输入 `Git Blame Inline`
3. 选择 `Git Blame Inline: Toggle Inline Blame`
4. Blame 信息应该消失/重新出现

### 3. 修改代码并热重载（2 分钟）

#### 修改装饰样式

1. 打开 `src/decorationProvider.ts`
2. 找到第 101 行的显示格式：
   ```typescript
   const text = `${blameInfo.author}, ${relativeTime} • ${shortSummary}`;
   ```
3. 修改为：
   ```typescript
   const text = `👤 ${blameInfo.author} | ${relativeTime} | ${shortSummary}`;
   ```
4. 保存文件
5. 在扩展开发窗口按 `Cmd+R` / `Ctrl+R`
6. 查看变化！

#### 修改悬停样式

1. 打开 `src/hoverProvider.ts`
2. 修改 `createMarkdown` 方法中的内容
3. 保存并重载（`Cmd+R` / `Ctrl+R`）

### 4. 常见问题

#### ❌ 没有显示 Blame 信息？

检查：
- 文件是否在 Git 仓库中？
- 文件是否已提交？
- 打开开发者工具（`Help > Toggle Developer Tools`）查看控制台错误

#### ❌ 修改代码后没有生效？

- 确保保存了文件（`Cmd+S` / `Ctrl+S`）
- 在扩展开发窗口按 `Cmd+R` / `Ctrl+R` 重新加载
- 检查编译是否有错误（查看终端输出）

#### ❌ 扩展开发窗口无法启动？

- 确保已安装依赖：`bun install` 或 `npm install`
- 检查 `dist/extension.js` 是否存在
- 手动运行 `bun run build`

### 5. 下一步

- 📖 阅读 [DEVELOPMENT.md](DEVELOPMENT.md) 了解详细开发流程
- 🔧 查看 [src/](src/) 目录了解代码结构
- 🚀 运行 `bun run package` 打包扩展

## 快速命令参考

```bash
# 开发模式（自动编译）
bun run dev

# 构建生产版本
bun run build

# 打包为 .vsix 文件
bun run package

# 安装到 VSCode
code --install-extension git-blame-inline-0.0.1.vsix
```

## 键盘快捷键

| 操作           | 快捷键                         |
| -------------- | ------------------------------ |
| 启动调试       | `F5`                           |
| 重新加载扩展   | `Cmd+R` / `Ctrl+R`             |
| 打开命令面板   | `Cmd+Shift+P` / `Ctrl+Shift+P` |
| 打开开发者工具 | `Cmd+Shift+I` / `Ctrl+Shift+I` |

祝你开发愉快！🎉
